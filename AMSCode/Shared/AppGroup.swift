import Foundation

enum AppGroup {
    // Must match the "App Groups" capability enabled on both the app
    // and widget extension targets in Xcode > Signing & Capabilities.
    static let identifier = "group.com.example.amscode"
}
